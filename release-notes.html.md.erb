---
title: Release Notes
owner: Security Engineering
---

<strong><%= modified_date %></strong>

This topic contains release notes for <%= vars.product_full %> (<%= vars.product_abbr %>).

For product versions and upgrade paths, see [Upgrade Planner](https://upgrade-planner.pivotal.io/).

## <a id='releases'></a> Releases
### <a id='2-1-11'></a> v2.1.11

**Release Date**: September 15, 2022

#### Maintenance Changes

* Add Jammy support (#93)
* Add debug
* Add job to collect provenance metadata (#83)
* Download filesnitch deps when needed
* Final release 2.1.10
* Fix deploy worker script
* Fix offline compilation issue (#97)
* Fix script
* Harness carvel to simplify the work to keep up with TAS (#81)
* Make SATS use Toolsmiths envs (#80)
* Make fim pipeline more resilient to flakiness (#79)
* Prepare pipeline to be reset by meta-pipeline (#87)
* Remove unused vars
* Require windows-tools release to be passed in
* Revert "Stop compilation for debugging"
* Revert "Update filesnitch submodule url"
* Revert "debugging fim windows compilation"
* Revert "keep compilation vms around a bit more"
* Revert "stop compilation after things happened"
* Stop compilation for debugging
* Stop consuming PRs in cut-fim-release job (#96)
* Test Jammy compatibility (#90)
* Update pipeline to test the latest TAS (#89)
* Update pipeline to test the latest TAS (#94)
* Update pipeline to test the latest TAS (#95)
* Use git protocol for getting submodule (#85)
* [DO NOT MERGE] Test without snowflake envs (#84)
* [ci] Add norsk status
* [ci] Remove unnecessary serial group
* [ci] Trigger integration tests new release/tile
* [ci] Upload stemcells needed by airgapped envs
* [ci] check for task  inputs
* [ci] pipeline now submits PR for release notes
* [ci] stop asking for OSM in this pipeline
* [ci] use non-admin creds for pivnet
* [deploy-windows-worker] create fim release on the fly
* [deploy-windows-worker] fix identation issues
* [deploy-windows-worker] make blobstore creds available in env
* [deploy-windows-worker] re-introduce fim release
* [deploy-windows-worker] try without fim
* [deploy-windows-worker] update fim-release path
* [filesnitch-windows] make GOMODCACHE writeable
* [provenance] Add revision
* debugging fim windows compilation
* keep compilation vms around a bit more
* stop compilation after things happened

#### Dependencies

* **filesnitch:** Updated to v`55e498b`.<br>For more information, see [filesnitch](https://github.com/pivotal-cf/filesnitch).



### <a id='2-1-10'></a> v2.1.10

**Release Date**: March 8, 2022

#### Maintenance Changes

Maintenance changes in this release:

* Validated compatibility with <%= vars.app_runtime_full %> v2.12.

#### Known Issues

This release has the following known issue:

* `user` and `group` security metadata might not be accurate for files in containers on
  <%= vars.app_runtime_full %> (<%= vars.app_runtime_abbr %>).
  However, you can determine the correct username and group name for a file from the `uid` and `gid`
  metadata keys.
  For more information, see [Metadata](installing.html#metadata).


### <a id="2-1-8"></a> v2.1.8

**Release Date: October 28, 2021**

#### Features

New features and changes in this release:

* **New patches for Go:** Adds latest patches for all Go dependencies
* **Golang update:** Updates golang dependency to v1.16 for both Linux and Windows

#### Known Issues

This release has the following known issue:

* `user` and `group` security metadata might not be accurate for files in containers on
  <%= vars.app_runtime_full %> (<%= vars.app_runtime_abbr %>).
  However, you can determine the correct username and group name for a file from the `uid` and `gid`
  metadata keys.
  For more information, see [Metadata](installing.html#metadata).

### <a id="2-1-5"></a> v2.1.5

## <a id='releases'></a> Releases
### <a id='2-1-11'></a> v2.1.11

**Release Date**: September 15, 2022

#### Maintenance Changes

* Add Jammy support (#93)
* Add debug
* Add job to collect provenance metadata (#83)
* Download filesnitch deps when needed
* Final release 2.1.10
* Fix deploy worker script
* Fix offline compilation issue (#97)
* Fix script
* Harness carvel to simplify the work to keep up with TAS (#81)
* Make SATS use Toolsmiths envs (#80)
* Make fim pipeline more resilient to flakiness (#79)
* Prepare pipeline to be reset by meta-pipeline (#87)
* Remove unused vars
* Require windows-tools release to be passed in
* Revert "Stop compilation for debugging"
* Revert "Update filesnitch submodule url"
* Revert "debugging fim windows compilation"
* Revert "keep compilation vms around a bit more"
* Revert "stop compilation after things happened"
* Stop compilation for debugging
* Stop consuming PRs in cut-fim-release job (#96)
* Test Jammy compatibility (#90)
* Update pipeline to test the latest TAS (#89)
* Update pipeline to test the latest TAS (#94)
* Update pipeline to test the latest TAS (#95)
* Use git protocol for getting submodule (#85)
* [DO NOT MERGE] Test without snowflake envs (#84)
* [ci] Add norsk status
* [ci] Remove unnecessary serial group
* [ci] Trigger integration tests new release/tile
* [ci] Upload stemcells needed by airgapped envs
* [ci] check for task  inputs
* [ci] pipeline now submits PR for release notes
* [ci] stop asking for OSM in this pipeline
* [ci] use non-admin creds for pivnet
* [deploy-windows-worker] create fim release on the fly
* [deploy-windows-worker] fix identation issues
* [deploy-windows-worker] make blobstore creds available in env
* [deploy-windows-worker] re-introduce fim release
* [deploy-windows-worker] try without fim
* [deploy-windows-worker] update fim-release path
* [filesnitch-windows] make GOMODCACHE writeable
* [provenance] Add revision
* debugging fim windows compilation
* keep compilation vms around a bit more
* stop compilation after things happened

#### Dependencies

* **filesnitch:** Updated to v`55e498b`.<br>For more information, see [filesnitch](https://github.com/pivotal-cf/filesnitch).



### <a id='2-1-10'></a> v2.1.10

**Release Date**: February 18, 2022

#### Maintenance Changes

Maintenance changes in this release:

* Validated compatibility with <%= vars.app_runtime_full %> v2.12.
* Various internal technical improvements, for example, to the CI.

#### Dependencies

This release updates the following dependencies:

* **filesnitch:** Updated to v`3017d46`.<br>
For more information, see [filesnitch](https://github.com/pivotal-cf/filesnitch) in GitHub.

#### Known Issues

This release has the following known issue:

* `user` and `group` security metadata might not be accurate for files in containers on
  <%= vars.app_runtime_full %> (<%= vars.app_runtime_abbr %>).
  However, you can determine the correct username and group name for a file from the `uid` and `gid`
  metadata keys.
  For more information, see [Metadata](installing.html#metadata).


### <a id="2-1-8"></a> v2.1.8

**Release Date: October 28, 2021**

#### Features

New features and changes in this release:

* **New patches for Go:** Adds latest patches for all Go dependencies
* **Golang update:** Updates golang dependency to v1.16 for both Linux and Windows

#### Known Issues

This release has the following known issue:

* `user` and `group` security metadata might not be accurate for files in containers on
  <%= vars.app_runtime_full %> (<%= vars.app_runtime_abbr %>).
  However, you can determine the correct username and group name for a file from the `uid` and `gid`
  metadata keys.
  For more information, see [Metadata](installing.html#metadata).

### <a id="2-1-5"></a> v2.1.5

**Release Date: January 25, 2021**

#### Features

New features and changes in this release:

* General Availability of <%= vars.product_abbr %> for Windows.
* Updates golang dependency to v1.15 for both Linux and Windows.

#### Known Issues

This release has the following known issue:

* `user` and `group` security metadata might not be accurate for files in containers on
<%= vars.app_runtime_full %> (<%= vars.app_runtime_abbr %>).
However, you can determine the correct username and group name for a file from the `uid` and `gid`
metadata keys.
For more information, see [Metadata](installing.html#metadata).

### <a id="2-1-3"></a> v2.1.3

**Release Date**: May 20, 2020

#### Features

New features and changes in this release:

* **Adds the BOSH Context Adaptor for <%= vars.product_abbr %> (BCAF):**
If you enable this feature, BCAF detects BOSH-triggered events and lowers the severity in the log to
`3`.
For more information, see
[BOSH Context Adaptor for <%= vars.product_short %> (BCAF)](index.html#bcaf).

* **Adds support for an in&#8209;memory cache on Linux to store security metadata for files:**
If you enable this feature, <%= vars.product_abbr %> uses more memory, but reduces the number of
trivial file events that are logged.
For more information, see [In-Memory Cache](installing.html#in-memory-cache).

* Adds security metadata for `CREATE` and `CHMOD` events on Linux.
For more information, see [Metadata](installing.html#metadata).

#### Known Issues

This release has the following known issue:

* `user` and `group` security metadata might not be accurate for files in containers on
<%= vars.app_runtime_first %>.
However, you can determine the correct username and group name for a file from the `uid` and `gid`
metadata keys.
For more information, see [Metadata](installing.html#metadata).

## <a id="view"></a> View Release Notes for Another Version

To view the release notes for another product version, select the version from dropdown at the top of this page.
