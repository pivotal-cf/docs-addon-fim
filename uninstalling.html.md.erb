---
title: Uninstalling FIM Add-on for PCF
owner: Security Engineering
---

<strong><%= modified_date %></strong>

This topic describes how to uninstall the File Integrity Monitoring Add-on for PCF (FIM Add-on) from your deployment, and how to verify the uninstallation.

## <a id='uninstall-fim'></a>Uninstall FIM Add-on

1. Retrieve the latest runtime config by running the following command:
  <pre>
    $ bosh -e my-env runtime-config > PATH-TO-SAVE-THE-RUNTIME-CONFIG
  </pre>
2. In the runtime config, remove all FIM properties under the `releases:` and `addons:` sections.

3. Update the runtime config.
  <pre>
    $ bosh -e my-env update-runtime-config --name=fim PATH-TO-SAVE-THE-RUNTIME-CONFIG
  </pre>
4. Navigate to your **Installation Dashboard** in Ops Manager.

5. If you are using Ops Manager v2.3 or later, click **Review Pending Changes**.
   For more information about this Ops Manager page,
   see [Reviewing Pending Product Changes](https://docs.pivotal.io/pivotalcf/customizing/review-pending-changes.html).

6. Click **Apply Changes**.

7. Wait for the installation to complete.

## <a id='verify-uninstall'></a>Verify the Uninstallation

<ol>
<li><a href="https://docs.pivotal.io/pivotalcf/customizing/trouble-advanced.html#bosh-ssh">BOSH SSH</a> 
  into one of the VMs in your deployment.
</li>
<li>Run <code>monit summary</code>.<br>
    If FIM has uninstalled successfully,
    it should not show the <code>fim</code> process.</li>
</ol>
